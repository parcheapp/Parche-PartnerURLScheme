//This is the build.gradle file for JitPack. 

allprojects {
	version = "android-1.0.0"
}

subprojects {
	apply plugin: 'java'
	apply plugin: 'maven'
	
	sourceCompatibility = 1.7
	targetCompatibility = 1.7
}